# Daylist

A polished, mobile-first task manager built with React and Vite. Daylist stores
tasks and appearance preferences locally in the browser, and supports searching,
filtering, editing, completion tracking, and light or dark themes.

## Development

```bash
npm install
npm run dev
```

Create a production bundle with `npm run build`.
